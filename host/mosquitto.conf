# mosquitto.conf - broker MQTT do backend (host)
# escuta em todas as interfaces (pra aceitar conexoes via WireGuard)
# e permite acesso anonimo (uso em rede privada / demo)
listener 1883 0.0.0.0
allow_anonymous true
